# molecule-cosmetics

Cosmetic items and effects (SPEC §24, §44).

## Status: not implemented

Scheduled for **Phase 12** of the plan in [`docs/SPEC.md`](../docs/SPEC.md) §63.

This module is registered in the build so the dependency graph, CI and release
pipeline are complete from the start, but it contains no implementation yet. The
`.scaffold` marker in this directory excludes it from releases — remove it once
there is a working plugin main class here.
